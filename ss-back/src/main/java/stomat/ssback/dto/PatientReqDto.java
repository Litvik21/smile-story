package stomat.ssback.dto;

public record PatientReqDto(Long generalInfoId,
                            Long  wishesMedicationId,
                            Long photoId,
                            Long scanId) {
}
